# Branch 9W3C Forbidden Actions

- Modify files listed in `FROZEN.lock`.
- Add a public command that creates a new Commander investigation.
- Claim exact resume, replay provider requests, or replay historical tool execution.
- Add retry, second-attempt, approval-revocation, or approve-and-execute authority.
- Enable the broad public Commander provider loop.
- Register approval, execution, or cancellation as Commander tools.
- Add GitHub/MCP reads, proposal generation, mission mutation, or OpenCode actions.
- Treat TUI state, preview IDs, or "latest" state as recovery authority.
- Report an operation as cancelled merely because an abort was requested.
- Serialize execute and cancel so cancellation cannot reach an active operation.
- Skip, xfail, deselect, rename, weaken, or hide a failing test.

